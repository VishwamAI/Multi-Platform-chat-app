import { ParsedIceCandidate, SimpleCallTest } from './callTest';
export default class ConnectivityTest extends SimpleCallTest {
    ch1: RTCDataChannel;
    ch2: RTCDataChannel;
    constructor(config: RTCConfiguration, filter?: (parsed: ParsedIceCandidate) => boolean);
    protected onCh1Recieve(event: MessageEvent): void;
    protected onCh2Recieve(event: MessageEvent): void;
    protected dataChannelHandler(event: RTCDataChannelEvent): void;
    protected hangup(errorMessage?: string): void;
}
export declare class HostConnectivityTest extends ConnectivityTest {
    constructor(config: RTCConfiguration);
}
export declare class RelayConnectivityTest extends ConnectivityTest {
    constructor(config: RTCConfiguration);
}
export declare class ReflexiveConnectivityTest extends ConnectivityTest {
    constructor(config: RTCConfiguration);
}
