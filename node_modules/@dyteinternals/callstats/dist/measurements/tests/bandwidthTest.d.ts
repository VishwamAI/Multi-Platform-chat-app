import { SimpleCallTest } from './callTest';
export default class DataThroughputTest extends SimpleCallTest {
    senderChannel: RTCDataChannel;
    recieveChannel: RTCDataChannel;
    startTime: Date;
    lastBitrateMeasureTime: Date;
    sentPayloadBytes: number;
    recievedPayloadBytes: number;
    lastReceivedPayloadBytes: number;
    stopSending: boolean;
    testProgress: number;
    samplePacket: string;
    finalBitrateSum: number;
    bitRateSampels: number;
    maxNumberOfPacketsToSend: number;
    bytesToKeepBuffered: number;
    testDurationSeconds: number;
    constructor(config: RTCConfiguration);
    protected sendingStep(): void;
    protected onMessageRecieved(event: MessageEvent): void;
    testComplete(data: {
        throughput: number;
    }): void;
    protected onRecieverChannel(event: RTCDataChannelEvent): void;
}
