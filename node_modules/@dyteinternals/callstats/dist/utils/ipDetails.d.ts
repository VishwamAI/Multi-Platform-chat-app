import { IPDetails } from '../measurements/types';
declare class IPDetailsManager {
    private ipInformation;
    getIPDetails({ peerId }: {
        peerId: string;
    }): Promise<IPDetails>;
}
declare const ipDetails: IPDetailsManager;
export default ipDetails;
